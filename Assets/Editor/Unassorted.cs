using UnityEditor;
using UnityEngine;

#if UNITY_EDITOR

public class Unassorted { //: BaseHierarchySort {

    /*public override int Compare( GameObject lhs, GameObject rhs ) {

        if( lhs == rhs ) return 0;
        if( lhs == null ) return 0;
        if( rhs == null ) return 0;

        return EditorUtility.NaturalCompare( lhs.name, rhs.name );
    }*/
}

#endif